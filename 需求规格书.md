我想仿照一个开源项目，做一个「月兔娘」桌面悬浮窗挂件，盯我的
Kimi Coding Plan 额度。

参考项目：https://github.com/MeteorNOX/DeepSeek-Balance-Whale-Widget
（DeepSeek Harness 的余额挂件：常驻右下角的小鲸鱼娘，盯 API 余额。
它是 MIT 协议，可借鉴但成品中需保留原作者署名与许可说明。）

【第一步：调研】
1. 完整阅读参考仓库，重点是 README.md、lib/index.js 和
   whale-widget-prompt.md（作者写的完整规格文档），梳理出它的
   全部功能清单和交互细节。
2. 调研 Kimi Coding Plan 的额度体系与可用的数据获取途径。已知背景：
   - 订阅制，额度每 7 天自动刷新，未用完不累积
   - 另有每 5 小时的滚动频率窗口，短时间请求过多会限流
   - 官方查询途径：Kimi Code CLI 里输入 /usage；Kimi Code 控制台网页
   - 订阅额度用尽后有「加油包」（人民币余额）兜底
   - 注意：Kimi Code 没有公开的余额查询 API，不要假设存在
   `api.moonshot.cn/v1/users/me/balance` 之外的接口，也不要把
   开放平台的按量计费余额当成 Coding Plan 额度——两者是两套体系。

【第二步：先交方案，不要写代码】
输出一份适配方案，包含：
1. 功能映射表：原项目每个功能 → 本项目的实现方式 → 无法移植的项及原因
2. 数据获取方案对比：至少评估这三条路的可行性——
   a) 调用本地 Kimi Code CLI 的 /usage（或非交互等价命令）解析输出
   b) 本地记账模式：监听/估算本会话 usage，自行累计本周用量
   c) 控制台网页接口（需要登录态令牌，仿照原项目的"令牌模式"做成可选）
   给出推荐的主备组合。
3. 桌面悬浮窗技术选型（如 Tauri / Electron / PyQt），要求 Windows
   可用、支持透明无边框置顶窗口、体积小。
我确认方案后再开始写代码。

【第三步：实现要求】
显示内容：
- 本周额度已用/剩余（进度条 + 百分比），下次刷新倒计时
- 5 小时滚动窗口状态
- 加油包余额（如数据源可得，否则菜单里隐藏该项）
- 每轮对话消耗估算（可开关）

交互与动效（对齐原项目）：
- 默认右下角常驻，60 秒自动刷新 + 点击兔娘手动刷新
- 数字变化时滚动动画
- 拖拽 + 四边四分之一吸附，左吸附时整体水平镜像翻转
- 按压 Q 弹效果（按压时底部坐标不变）
- 汉堡菜单：大小滑块、音效开关与音量、气泡开关、数据源切换
- 随机台词气泡：加权随机，5 秒自动收起

形象与文案：
- 形象为「月兔娘」：住月球暗面的兔耳女孩，银白发蓝紫渐变，
  透明背景 cut-out PNG（我会提供素材，开发期先用占位图）
- 配色：暗夜蓝紫 + 月光银，不要深海蓝
- 台词口吻：安静、爱读书、熬夜陪用户写代码，额度快没时会
  着急（"本周额度只剩 8% 了，要省着点用我吗……"）

工程要求：
- 数据源全部做成可插拔适配层，CLI 解析失败时自动降级到记账模式，
  不报错崩溃
- 配置文件本地存储，令牌等敏感信息不明文落盘
- README 写明：安装方式、数据获取原理、各模式的配置方法、
  与原项目的致谢

【第四步：交付到 GitHub】
代码完成并本地验证通过后：
1. git init 并写好 .gitignore（排除 token、配置、构建产物）
2. 用 gh CLI 创建公开仓库 kimi-rabbit-widget（如果 gh 不可用，
   停下来告诉我，我手动建仓库后再继续）
3. README 用中文撰写，包含：效果图占位、功能列表、安装步骤、
   数据源原理说明、与参考项目的致谢及 MIT 许可
4. 推送 main 分支，并把仓库地址告诉我
注意：任何 API Key、登录令牌、我的个人额度数据都不允许提交。
